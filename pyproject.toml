[project]
name = "jev-langchain-harness-poc"
version = "0.1.0"
description = "Minimal PoC: Jev (TypeSafe) classifier + LangChain agent harness middleware"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3",
    "langchain-openai>=0.2",
    "langchain-typesafe[experimental]>=0.0.1a2",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
