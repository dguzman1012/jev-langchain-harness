# Required for Jev / TypeSafe (classifier + middleware)
TYPESAFE_API_KEY=

# Optional: full create_agent demo with OpenAI models (omit for --dry-run harness)
OPENAI_API_KEY=

# Optional: override TypeSafe API base URL (default https://api.typesafe.ai)
# TYPESAFE_BASE_URL=
